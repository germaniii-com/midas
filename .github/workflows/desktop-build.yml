name: Desktop Build

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  packages: write

env:
  NODE_VERSION: "20"

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, ubuntu-latest, windows-latest]
        include:
          - os: macos-latest
            build-cmd: package:mac
            artifact-glob: desktop/release/*.dmg
            artifact-name: Midas-macOS

          - os: ubuntu-latest
            build-cmd: package:linux
            artifact-glob: desktop/release/*.AppImage
            artifact-name: Midas-Linux

          - os: windows-latest
            build-cmd: package:win
            artifact-glob: desktop/release/*.exe
            artifact-name: Midas-Windows

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout Code
        shell: bash
        run: |
          git config --global init.defaultBranch main
          git init
          git remote add origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git

          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            git fetch --depth=1 origin +refs/pull/${{ github.event.pull_request.number }}/merge:refs/remotes/origin/pr
            git checkout -q origin/pr
          else
            git fetch --depth=1 origin ${{ github.sha }}
            git checkout -q ${{ github.sha }}
          fi

      - name: Setup Node.js
        shell: bash
        run: |
          # Use nvm on Linux/macOS, fallback to system Node.js on Windows
          if command -v nvm &>/dev/null; then
            nvm install $NODE_VERSION
            nvm use $NODE_VERSION
          elif command -v fnm &>/dev/null; then
            fnm install $NODE_VERSION
            fnm use $NODE_VERSION
          fi
          echo "node $(node --version)"
          echo "npm v$(npm --version)"

      - name: Install Linux system dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq \
            libgtk-3-0 libnotify4 libnss3 libxss1 \
            libxtst6 xdg-utils libatspi2.0-0 \
            libdrm2 libgbm1 \
            fuse rpm \
            libasound2t64 2>/dev/null || \
          sudo apt-get install -y -qq \
            libgtk-3-0 libnotify4 libnss3 libxss1 \
            libxtst6 xdg-utils libatspi2.0-0 \
            libdrm2 libgbm1 \
            fuse rpm \
            libasound2 libfuse2 2>/dev/null

      - name: Install project dependencies
        run: npm ci

      - name: Build backend
        run: npm run build --workspace=backend

      - name: Build frontend
        run: npm run build --workspace=frontend

      - name: Build & package desktop app
        run: npm run ${{ matrix.build-cmd }} --workspace=desktop
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: List release artifacts
        shell: bash
        run: |
          if [ -d desktop/release ]; then
            find desktop/release -type f -ls
          else
            echo "No release directory found"
          fi

      - name: Upload to GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          VERSION="${GITHUB_REF#refs/tags/}"
          echo "Creating release $VERSION ..."

          # Create release
          gh release create "$VERSION" \
            --title "$VERSION" \
            --generate-notes \
            --repo ${{ github.repository }} || echo "Release may already exist"

          # Upload artifacts (shell expands wildcard to matched files)
          for f in ${{ matrix.artifact-glob }}; do
            if [ -f "$f" ]; then
              echo "Uploading $f ..."
              gh release upload "$VERSION" "$f" \
                --repo ${{ github.repository }} --clobber
            fi
          done
